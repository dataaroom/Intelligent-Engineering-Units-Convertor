# 题目：将一个正整数分解质因数。例如：输入90,打印出90=2*3*3*5


def zhishu(n):    
  
  return (shulist)   # return a list including all prime numbers from 2 to n.
  
while number 
number = input('Please give a number (interger only)')
prime = []
primeNumber = zhishu(number)
for i in primeNumber:
  if number % i == 0:
      number /= i
      prime.append(i)

print('%s = %s', % (number, prime[0]), end = '')
for j in range(1:len(prime)):
    for print(' * %s', % prime[j])
    
      
